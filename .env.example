# Firebase Admin SDK - Credenciales de servicio
# Descarga el JSON desde Firebase Console > Project Settings > Service Accounts > Generate new private key
# Guarda el archivo como service-account.json en la raiz del proyecto
GOOGLE_APPLICATION_CREDENTIALS="./service-account.json"

# Entorno: development | production
NODE_ENV=development
